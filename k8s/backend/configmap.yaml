apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: fullstack
data:
  MINIO_ENDPOINT: "minio:9000"
  # deploy.sh patches this automatically with: http://<minikube-ip>:30090
  MINIO_PUBLIC_URL: "http://192.168.49.2:30090"
