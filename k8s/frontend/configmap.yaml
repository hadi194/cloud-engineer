apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-config
  namespace: fullstack
# No env vars needed — Nginx inside the container handles backend proxying.
# Backend URL is hardcoded in nginx.conf: proxy_pass http://backend:8080
data: {}
